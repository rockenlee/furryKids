import SwiftUI

struct ContentView: View {
    var body: some View {
        Text("毛孩子AI")
            .font(.largeTitle)
            .fontWeight(.bold)
            .foregroundColor(.blue)
            .padding()
    }
}

#Preview {
    ContentView()
} 